#! /bin/bash

git remote update --prune

deletedBranches() {
  git branch -vv --no-color | grep ': gone]'|  grep -v "\*" | awk '{ print $1; }'
}

if [[ $1 == "--force" || $1 == "-f" ]]; then
  deletedBranches | xargs -r git branch -D
else
  deletedBranches | xargs -r git branch -d
fi

