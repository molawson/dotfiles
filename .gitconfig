[user]
	name = Mo Lawson
	email = moklawson@gmail.com
[color]
  ui = auto
[core]
	quotepath = false
[alias]
  st = status
  ci = commit
  co = checkout
  di = diff
  dc = diff --cached
  dis = !git di --stat=100,100
  dcs = !git dc --stat=100,100
  amend = commit --amend
  aa = add --all
  ap = add -p
  head = !git l -1
  h = !git head
  r = !git l -20
  ra = !git r --all
  ru = remote update
  p = push
  po = !git p origin
  u = pull
  uo = !git u origin
  ff = merge --ff-only
  noff = merge --no-ff
  uff = !git u --ff-only
  l = log --graph --abbrev-commit --date=relative
  la = !git l --all
  div = divergence
  gn = goodness
  gnc = goodness --cached
  fa = fetch --all
  b = branch
  ds = diff --stat=160,120
  dh1 = diff HEAD-1
  ss = stash save
  sp = !git ss --patch
  so = stash pop
  sdc = svn dcommit
  sfa = svn fetch --all
  sr = svn rebase
[format]
  pretty = format:%C(yellow)%h%Creset -%C(red)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset
[merge]
  tool = vimdiff
[mergetool]
	keepBackup = true
